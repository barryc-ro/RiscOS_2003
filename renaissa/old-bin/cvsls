#! /usr/local/bin/perl
$CVSROOT=$ENV{'CVSROOT'}.'/';
for($i=0;$i<=$#ARGV;$i++) {
  last if($ARGV[$i]!~/^-/);
};
$ARGV[$i]='.' if($i>$#ARGV);
for(;$i<=$#ARGV;$i++) {
  $ARGV[$i]=$CVSROOT.$ARGV[$i];
};
#print join(':',@ARGV),"\n";
exec('ls',@ARGV);
