#! /usr/local/bin/perl
# Usage: cvsls [options] directory
$CVSROOT=$ENV{'CVSROOT'}.'/';
for($i=0;$i<=$#ARGV;$i++) {
  last if($ARGV[$i]!~/^-/);
};
$ARGV[$i]='.' if($i>$#ARGV);
for(;$i<=$#ARGV;$i++) {
  $ARGV[$i]=$CVSROOT.$ARGV[$i];
};
#print join(':',@ARGV),"\n";
exec('ls',@ARGV);
