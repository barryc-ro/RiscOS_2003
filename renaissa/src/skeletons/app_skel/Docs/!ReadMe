!ReadMe

These notes have been put together to provide a skeleton C/OBJASM 
application structure that fits into the Acorn build mechanism.

The example C module given does the following :
* Uses toolbox paths and libraries
* Pulls the version number and date from the VersionNum file into
  a ProgInfo window.