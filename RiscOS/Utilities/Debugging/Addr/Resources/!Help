Addr
====
Addr is a very simple application that describes the location of an address
in memory.

Syntax: 

  *Addr <address>
     - Display location of address given
  *Addr -e
     - Display list of 'entities' (single value entry points). At present
       only Software vectors are defined.
  *Addr -m
     - Display the known memory map.
       Note: This is big.
  *Addr -h
     - Display help message

-- 
Justin Fletcher
