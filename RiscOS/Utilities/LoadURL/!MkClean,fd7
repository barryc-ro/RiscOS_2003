| Remove executables, objects and intermediate files

Echo Cleaning...

Dir <Obey$Dir>
amu_machine TARGET=WorkAroundAMUBug clean INSTDIR=Targets.!LoadURL

ifthere Run:stripdepnd then stripdepnd -v <Obey$Dir>.Makefile
| Strip CVS files from NCMail.  Requires !EraseCVS 0.02.
If "<Alias$EraseCVS>" <> "" then EraseCVS else Echo **EraseCVS not found.

