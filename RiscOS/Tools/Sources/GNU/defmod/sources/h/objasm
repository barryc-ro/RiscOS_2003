/*objasm.h - output an objasm file given a module defn*/
#ifndef objasm_H
#define objasm_H

#include <stdio.h>

extern os_error *objasm_output (FILE *, char *, char *, lookup_t, lookup_t,
      lookup_t, lookup_t, osbool, osbool, char *output);

#endif
