# Simple substitution tests

OBJS = foo.o bar.o yelp.o
ROMOBJS := ${OBJS:%.o=or.%}

BAR = ex

all:
  @if "${ROMOBJS}" <> "or.foo or.bar or.yelp" Then Error "Test ST-01 failed"
  @|
  @echo All tests passed
