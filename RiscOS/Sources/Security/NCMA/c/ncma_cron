/* ncma_cron.c - handle Managed access interface to the cron scheduler */
#include <stdio.h>
#include <string.h>

#define PROTOTYPES_INTERNAL
#include "ncma_cron.h"
#include "DBUG.h"

int ncma_cron_kick(void)
{
    DBUG_PRINTF(DBUG_LVL_ERROR, "ncma_cron_kick: Neil! Where's cron?!\n");
    return(0);
}
