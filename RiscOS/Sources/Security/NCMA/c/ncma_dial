/* ncma_dial.c - handle interface to NCDialUI stuff */
#include <stdio.h>
#include "kernel.h"
#include "swis.h"

#define PROTOTYPES_INTERNAL
#include "ncma_dial.h"

int ncma_dial(void)
{
  _kernel_swi_regs r;

  r.r[0] = 0;
  if(_kernel_swi(NCDialUI_Start, &r, &r)!=NULL) return(-1);

  return(0);
}

