// usb.h

// returns the chip ID from the ISP1161
short usb_chip_id (void);

// reads a device descriptor from an attached device, and writes if to the
// buffer provided
void usb_device_descriptor (char*);
