//support.h

#define FAILED 0
#define PASSED 1

// Specifiy the various areas of memory we can get information on
#define DRAM_AREA       1
//#define VRAM_AREA       2
//#define ROM_AREA        3
//#define IO_AREA         4

// define the different types of test mode we can have
// included for compatability with old UnitTest code
#define MODE_USER       0
#define MODE_LIFE       1
#define MODE_LIMIT      1

int find_memory_size(int area);
void wait(int cent);
int ahextoi(char *);
char *get_os_version(void);



