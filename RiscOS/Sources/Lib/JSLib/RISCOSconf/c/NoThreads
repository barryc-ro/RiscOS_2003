#include <stdlib.h>
#include <string.h>

#include "prtypes.h"
#include "prerror.h"

/* Don't need the thread library just yet - this gets around */
/* the use of PR_SetError by some bits and pieces.           */

PR_EXTERN(void) PR_SetError(PRErrorCode errorCode, PRInt32 oserr)
{
  PRErrorCode dosomething = errorCode;

  dosomething = dosomething;
}
