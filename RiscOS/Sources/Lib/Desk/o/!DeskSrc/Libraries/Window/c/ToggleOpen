#include "Desk.Window.h"

void	Desk_Window_ToggleShow( Desk_window_handle window, Desk_window_openpos openpos)
	{
	if ( Desk_Window_IsOpen( window))	Desk_Window_Hide( window);
	else					Desk_Window_Show( window, openpos);
	}
