Objects	=	\
#Makatic{
-F
-f "c.* -x ..CVS" o.#0\
#Makatic}

.SUFFIXES:	.o .c

All:	clxlib

clean:
	wipe @.o.* ~c~rv

clxlib:	$(Objects)
	libfile -c -o $@ $(Objects)

.c.o:
	cc -c -fah -depend !Depend -throwback -I,C: -o $@ $<

# Dynamic dependencies:
