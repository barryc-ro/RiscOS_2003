Set WWWRoot$Path Resources:$.!Boot.HTML.Startup.
SetMacro WWWDyn$Path <Wimp$ScrapDir>.Sennen.

| Loading DeviceFS from the ISDN ROM blows away the smartcard serial
| interface's open file handles, so reinitialise the smartcard stack.
RMReinit Serial
RMreinit SCTransport -fullinit
RMreinit SCInterface

Desktop Resources:$.Apps.!Sennen.!Run
