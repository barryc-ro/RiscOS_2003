/*	Flush Callbacks
	---------------

This routine drops into user mode and then executes a null SWI
to flush all pending callbacks 
*/

_kernel_oserror *usermode_donothing(void);

#define flush_callbacks() usermode_donothing()

