#include "swis.h"


int main(void)
{
  char buffer[260];
  int found = 0, count = 0;

  while (found != -1)
  {
    _swix(OS_GBPB, _INR(0, 6) | _OUT(4), 12, "ADFS::Devon.$.Omega.Caradon.c", buffer, 1, count, 255, "*", &found);
    if (found != -1)
      printf("%s\n", buffer + 24);
    count = found;
  }
}
