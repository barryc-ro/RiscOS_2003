Targets:

	Below is a list of all the targets currently available.

MkRom		Make a ROM version of the module.
MkMemC		Make a MemCheck-enabled version of the module.
MkMcCabe	Create a series of preprocessed listings ready to be put into McCabe.
Mk		Make a standalone version of the module.
MkClean		Clean the module and all associated files.
MkDebug		Make a DebugLib-enabled version of the module.
MkDocs		Make documents describing the code.
MkInst		Instrument code which has been run though McCabe to make a standalone module.
