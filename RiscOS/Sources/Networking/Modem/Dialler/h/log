/* File:    log.h
 * Purpose: Interface to log.c
 * Author:  DN Walker, Ben Laughton
 * History:
 *
 * 1996-12-18: BAL
 * Inherited.
 */

#ifndef Dialler_log_h
#define Dialler_log_h

/* =============================================================================
 * Include header files
 */

/* CLib */
#include <stdio.h>

#if defined DEBUG_TML
  #include "SVCPrint/svcprint.h"
#elif defined PDebug_DEBUG
  #include "PDebug/Send.h"
#endif

#ifdef Trace_TRACE
  #include "Trace/Trace.h"
#endif



/* =============================================================================
 * Define macros
 */

#ifdef DEBUG
  #define M_debug(a) log_output a
#else
  #define M_debug(a)
#endif




/* =============================================================================
 * Declare global variables
 */

extern char logfile_name[];




/* =============================================================================
 * Declare function prototypes
 */

extern void logf (const char *const str);
extern void syslog (const char *format, ...);
extern void printlog (void);
extern void syslogflush (void);

#if defined Trace_TRACE || defined DEBUG
  extern void log_output (char *format, ...);
  extern void log_trace_level (void);
  extern void log_trace_enter (const char *fn);
  extern void log_trace_exit (const char *fn);
  extern void log_trace_reset (void);
#endif

#endif
