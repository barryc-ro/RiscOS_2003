/*--------------------------------------------------------------------------*
 *                       M O D U L E   H E A D E R
 *
 * filename - dl_pic.c    PIC XID meta-programs initialization functions.
 *
 * $Revision$
 *
 * $Date$
 *
 * function(s)
 *
 *	initialization of address fields inside Tables or meta-program for
 *	Position Independant Code Compilers. (each function called using "CALL"
 *	Macro is generated by each meta-program as described in xid_pic.h).
 *
 *-------------------------------------------------------------------------*/

/*[]---------------------------------------------------[]*/
/*|                                                     |*/
/*|    LAP-BRICKS                                       |*/
/*|    ISDN-BRICKS                                      |*/
/*|    X25-BRICKS                                       |*/
/*|                                                     |*/
/*|    Copyright (c) 1989-1997 by OMNITEL SA            |*/
/*|    All Rights Reserved.                             |*/
/*|                                                     |*/
/*[]---------------------------------------------------[]*/

#define ENTITY 		DL_D			/* V120 Data Link entity */
#define MODULO 		128				/* MODULO 128 */

#include "dl.h"
#include "dl_comm.h"

#if C_COMPILER == TC
#	pragma hdrstop
#endif

#if OM_CODE_TYPE == OM_PIC	/* specific functions used only for PIC */

/*--------------------------------------------------------------------------*/

#if DL_XID == ON
#	include "xid_pic.h"
#endif

#define INCLUDE_ONLY_META_PROGRAMS	ON

#if (DL_V120 == ON) && (DL_V120_XID_NEGOTIATION == ON) && (DL_XID == ON)
#	include "v120_xid.h"
#endif

/*--------------------------------------------------------------------------*/

void pic_init_dl ()
{
#if (DL_V120 == ON) && (DL_V120_XID_NEGOTIATION == ON) && (DL_XID == ON)
	CALL (XID_V120_S)
#endif
}

#endif

/*--------------------------------------------------------------------------*/

/*EOF*/
