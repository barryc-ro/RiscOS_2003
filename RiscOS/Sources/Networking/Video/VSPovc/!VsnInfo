Version Info for VSPovc (Acorn Oracle Video Client)
===================================================

v3.04a
------
Created by SCormie 06-Oct-98

Based on files in v3_04 with the following changes:

c.module:

Fixed callb_handler so that it doesn't call mnIdle() if the stream is closed.
Stops crash if ticker goes off during VSP_CloseStream and sets callback that
goes off when the SWI exits.

c.mzswrap

Fixed duplicate name problem (fix donated by Carl).

v3.04
-----
Various bug fixes.

Changed some parts to use proper makefiles instead of broken !Make files.

Upgraded LibIDL to new IDLs delivered on CD ROM OVS 3.0.3.0.  Now 3.03
compatible.

