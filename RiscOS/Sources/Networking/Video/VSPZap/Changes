10-Mar-98 TMD Version 0.04
 Added SWI SVB_ReadChannelNames, so that the player program when it starts
can force the SVB module to re-read the names.
 Stopped it printing "SDB_ENTITLE:" except when debugging
 Removed contents of zap_initstream routine (believed to cause buffer loss)
