; Title:   s.Stubs
; Purpose: Resolver interfaces
;

        AREA    Resolver_Interfaces,REL,CODE,READONLY
	GET	hdr.RegDefs
	GET	hdr.SwiNos

	EXPORT	dns_gethostbyname
	EXPORT	dns_gethost
	EXPORT	dns_getcache
	EXPORT	dns_cachecontrol

dns_gethostbyname
	STMFD	sp!, {link}
	SWI	XResolver_GetHostByName
	LDMFD	sp!, {pc}

dns_gethost
	STMFD	sp!, {link}
	SWI	XResolver_GetHost
	LDMFD	sp!, {pc}

dns_getcache
	STMFD	sp!, {link}
	SWI	XResolver_GetCache
	LDMFD	sp!, {pc}

dns_cachecontrol
	STMFD	sp!, {link}
	SWI	XResolver_CacheControl
	LDMFD	sp!, {pc}

        END
