#include <stdarg.h>
#include <stdio.h>
#include "tml_podule.h"

static char buffer[4096];

void _dprintf(char level[], char format[], ...)
{
  va_list          arg;	
  
  va_start(arg,format);
  vsprintf(buffer, format, arg);						
  Printf(buffer);
  va_end(arg);
}

