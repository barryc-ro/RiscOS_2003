/* -*-c-*- */

/* Map from a content type to a RISC OS file type.  -1 for an error */
int mime_map_to_riscos(char *ct);
/* Map from a RISC OS file type to a content type.  NULL for an error */
char *mime_map_from_riscos(int ft);
