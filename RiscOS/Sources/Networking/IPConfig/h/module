/*
 * IPConfig (module.h)
 *
 * Copyright (C) Acorn Computers Ltd. 1998
 */

extern void editconfig_service(void *);
extern _kernel_oserror *editconfig_initialise(void);

enum {
        keycheck_BOTH_ALTS,
        keycheck_BOTH_ALTS_AND_SHIFTS
};
extern int editconfig_check_keys(int);

extern int module_read_command_options(const char *cmd_tail);

enum checked_cli_flags {
        flag_RUN_EDITOR = 1,
        flag_NO_DO_BOOT = 2,
        flag_FORCE_BOOT = 4
};

extern int Strncmp_ci(const char *first, const char *second, size_t limit);
