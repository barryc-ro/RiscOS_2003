/*
 * The global entry points for dialler.c
 */

#ifndef __dialler_global__
#define __dialler_global__


/* functions */
extern void dialler_hangup(void);

/* event handlers for buttons on 'dialup' window */
extern TBOX_HANDLER(dialler_cont);
extern TBOX_HANDLER(dialler_stop);
extern TBOX_HANDLER(dialler_prefix);


#endif /* __dialler_global__ */
