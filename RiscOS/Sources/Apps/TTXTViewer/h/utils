#ifndef UTILS_H
#define UTILS_H

#include <stdlib.h>
#include <stdio.h>

int stricmp(char const *a, char const *b);
int strnicmp(char const *a, char const *b, size_t size);
/* Yeah, I know, this is no longer valid C code... */

char *strdup(char const *str);

size_t file_size(FILE *f);

#endif /* UTILS_H */
