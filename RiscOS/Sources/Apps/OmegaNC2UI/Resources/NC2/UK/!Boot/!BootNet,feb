| If NFSBoot is defined then execute it.
|
If "<NFSBoot$Dir>" <> "" Then Echo --- Executing <NFSBoot$Dir>.PreDesk on <Inet$BootServer>
If "<NFSBoot$Dir>" <> "" Then Obey <NFSBoot$Dir>.PreDesk on <Inet$BootServer>
| 
| If ARMBoot is defined then execute it.
If "<ARMBoot$Dir>" <> "" Then Echo --- Executing <ARMBoot$Dir>.PreDesk on <Inet$BootServer>
If "<ARMBoot$Dir>" <> "" Then Obey <ARMBoot$Dir>.PreDesk on <Inet$BootServer>
