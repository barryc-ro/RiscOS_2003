# > InetDBase:InetDConf

# This file determines which servers are run on which ports and also
# contains the server options.
# The syntax is one line per server, consisting of the fields:
#  <port number or service name> <protocol> <server name> <server options>
# Fields can be separated by any number of spaces and/or tabs.
# If in doubt, read the Configuration section of the Manual file.

# If the server is running, use the Reconfigure option after editing this
# file to ensure changes take effect immediately.

telnet	tcp	telnet	-n 5 -idle 900 -k 120 -auth

