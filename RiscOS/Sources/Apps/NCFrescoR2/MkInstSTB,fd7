| MkInstAll
| Make all the standalone variants of NCFresco2

Dir <Obey$Dir>

Set OldInstall$Dir <Install$Dir>
Set OldLocale <Locale>
Set OldUserIf <UserIf>

|Alias NCFrescoBuild set userif %%0|mset locale %%1|mmkdir |<install$dir>.%%0-%%1|mamu install TYPE=%%2 UI_VARIANT=<UI_Variant> THROWBACK=-throwback INSTDIR=<Install$Dir>.%%0-%%1

echo <install$dir>

Alias NCFrescoBuild set userif %%0|mset locale %%1|mmkdir |<install$dir>|mamu install TYPE=%%2 UI_VARIANT=<UI_Variant> THROWBACK=-throwback INSTDIR=<Install$Dir>

NCFrescoBuild STB400  USA  JXU
NCFrescoBuild STB400  UK   JXU

WimpSlot -min 1880k
|Do /<Install$Dir>.!NCFresco.!RunImage -version > <Install$Dir>.!Version 2> null:

Set Install$Dir <OldInstall$Dir>
Set Locale <OldLocale>
Set UserIf <OldUserIf>

Unset OldInstall$Dir
Unset OldLocale
Unset OldUserIf

| Remember to reset locale to UK after all this is complete else there will be trouble later on... 
set locale UK
