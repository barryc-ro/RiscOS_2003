#!/bin/sh

# usage: wantlink source dest
wantlink()
{
        source=$1
	dest=$2
	if [ ! -L $dest ];
	then
		echo ln -s $source $dest
		ln -s $source $dest
	fi
}

wantlink ../ncfresco/Makefile Makefile
