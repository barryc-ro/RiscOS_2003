#!/bin/sh
#
# If python is available on this system, run the sanity
# checks. If it is not available, point this out to the
# user and assume all is well.
#

which python > /dev/null

if [ $? = 1 ] ; then
	echo "NO PYTHON - NO SANITY CHECKS"
	exit 0
fi

./cvs-checks.py

# eof cvs-checks
