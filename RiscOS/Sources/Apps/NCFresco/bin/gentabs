#!/bin/sh

Z=0

get_bogus()
{
	while [ $Z -eq 0 ]
	do
		bin/gentab
		Z=$?
	done
}

get_bogus

exit 1

# Hardcoded A.html filename!

COUNT=10

while [ $COUNT -gt 0 ]
do
	echo "$COUNT failures left to generate"
	get_bogus

	$COUNT=$COUNT - 1
done
