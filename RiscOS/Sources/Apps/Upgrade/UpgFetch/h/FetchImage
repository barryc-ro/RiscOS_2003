#ifndef _FetchImage
#define _FetchImage


#define NULL_COUNTOUT 100000   // null countout in calls


// Global variables
float start_timer_value = 0;
float last_timer_value = 0;
int number_of_null_reads = 0;

int our_task_handle;


// Function Prototypes

/* function to check that the connection hasn't timed out. The server_timeout parameter specifies the timeout period in seconds */
void check_for_timeout(const int server_timeout);

/* function to read the time in seconds */
float read_time_in_seconds(void);

// reset the machine
extern int reset_machine(int event_code, ToolboxEvent *event,
                             IdBlock *id_block,void *handle);



#endif
