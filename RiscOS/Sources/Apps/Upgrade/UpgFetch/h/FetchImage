#ifndef _FetchImage
#define _FetchImage


#define NULL_COUNTOUT 100000   // null countout in calls


// Global variables
float start_timer_value = 0;
float last_timer_value = 0;
int number_of_null_reads = 0;

int our_task_handle;


// Function Prototypes

// function to check that the connection hasn't timed out
void check_for_timeout(void);

// function to read the time in seconds
float read_time_in_seconds(void);

// function to initialise the wimp
void initialise_wimp(void);

// reset the machine
extern int reset_machine(int event_code, ToolboxEvent *event,
                             IdBlock *id_block,void *handle);

unsigned int return_free_memory(void);


#endif
