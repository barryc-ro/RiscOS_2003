#ifndef _MESSAGES
#define _MESSAGES


// function prototypes

// initialise the messages file
int init_messages(void);

// lookup a message token
char *lookup_message_token(char *token);

// close the messages file
int close_messages_file(void);

#endif

