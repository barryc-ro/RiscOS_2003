/* > play_scode.h

 */

extern void soundcode_stop_playback(void);
extern int soundcode_is_buffer_empty(void);
extern void soundcode_give_data(void *buffer, int size);

extern os_error *soundcode_start_playback1(const char *sound_file, int n_channels, int bufsize);
extern void soundcode_start_playback2(int sample_rate);

extern os_error *soundcode_play_soundfile(const char *filename, int bufsize);


/* eof play_scode.h */
