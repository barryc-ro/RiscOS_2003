/* > rstream.h

 * © SJ Middleton, 1995

 */

#ifndef __rstream_h
# define __rstream_h

#ifndef __replay_H
# include "os:replay.h"
#endif

extern replay_catalogue_chunk *get_catalogue_chunk(replay_header *hdr, int chunk_num);

#if 0

extern int rstream_bytesavailable(rstream *r);
extern os_error *rstream_seek(rstream *r);
extern BOOL rstream_eof(rstream *r);

extern rstream rstream_audioset(unsigned int f, armovie_hdr *hdr, int frame);
extern rstream rstream_videoset(unsigned int f, armovie_hdr *hdr, int frame);
extern int rstream_read(rstream *r, void *buffer, size_t nbytes);
extern int rstream_write(rstream *r, const void *buffer, size_t nbytes);

#endif

#endif

/* eof rstream.h */
