/* bits.h */

//void open_bit_stream (char *name, unsigned int buffsize);
void mpeg_set_bit_stream (unsigned char *buffer, int bitoffset, int buffsize);
extern uint32  getbits (int);
extern uint32  bitposition (void);
//extern int ensure_bits (int);
extern int ensure_bits (int needbits/*, unsigned char **inputBuffer*/);
void close_bit_stream (void);

/* EOF bits.h */
