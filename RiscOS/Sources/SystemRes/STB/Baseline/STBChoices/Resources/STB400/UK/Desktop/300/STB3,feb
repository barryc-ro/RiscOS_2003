| If the system variables are set, get the time from a network time server, and set the STB time zone
If "<STB$TimeServer>" <> "" Then Set NetTime$Server <STB$TimeServer>
If "<STB$TimeZone>"   <> "" Then Do Configure TimeZone <STB$TimeZone>
If "<STB$TimeServer>" <> "" Then NetTime_Kick
