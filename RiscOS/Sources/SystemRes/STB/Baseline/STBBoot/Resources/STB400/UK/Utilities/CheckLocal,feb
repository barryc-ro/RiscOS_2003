| >!STBBoot.Utilities.CheckLocal
| © Pace Micro Technology plc, 1999

If BootRun$PreDesk     Then IfThere <STBPreDesk$Dir>     Then Else Set STBPreDesk$Dir     NetRoot:!STBBoot.PreDesktop
If BootRun$HookPreDesk Then IfThere <STBHookPreDesk$Dir> Then Else Set STBHookPreDesk$Dir NetRoot:!STBBoot.PreDesktop.<OS$Version>
If BootRun$Desktop     Then IfThere <STBDesktop$Dir>     Then Else Set STBDesktop$Dir     NetRoot:!STBBoot.Desktop
If BootRun$HookDesk    Then IfThere <STBHookDesk$Dir>    Then Else Set STBHookDesk$Dir    NetRoot:!STBBoot.Desktop.<OS$Version>
If BootBoot$HookRes    Then IfThere <STBHookRes$Dir>     Then Else Set STBHookRes$Dir     NetRoot:!STBBoot.Resources.<OS$Version>

If BootBoot$Apps OR BootRun$Apps Then IfThere <STBApps$Dir> Then Else Set STBApps$Dir NetRoot:Apps

IfThere <STBChoices$Dir>   Then Else Set STBChoices$Dir   NetRoot:!STBBoot.Choices
IfThere <STBHookMisc$Dir>  Then Else Set STBHookMisc$Dir  NetRoot:!STBBoot.Resources.<OS$Version>.Misc
IfThere <STBLibrary$Dir>   Then Else Set STBLibrary$Dir   NetRoot:!STBBoot.Library
IfThere <STBResources$Dir> Then Else Set STBResources$Dir NetRoot:!STBBoot.Resources
IfThere <STBUtilities$Dir> Then Else Set STBUtilities$Dir NetRoot:!STBBoot.Utilities
IfThere <STBVersions$Dir>  Then Else Set STBVersions$Dir  NetRoot:Versions
