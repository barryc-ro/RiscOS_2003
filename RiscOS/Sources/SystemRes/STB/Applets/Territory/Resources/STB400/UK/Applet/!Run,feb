If "<OS$Version>" = "" Then Obey

Set TerrConf$Dir  <Obey$Dir>
Set TerrConf$Path <Obey$Dir>.

RMCheck International         1.57 Inter
RMCheck InternationalKeyboard 0.75 IntKey
RMCheck TerritoryManager      0.34 TerrMgr

Run TerrConf:!RunImage

| The IntKey build included here only includes UK and USA layout so force the
| layout to USA here for non-uk versions.

If "<STB$Territory>" = "Canada1" Then Keyboard USA
If "<STB$Territory>" = "France"  Then Keyboard USA
