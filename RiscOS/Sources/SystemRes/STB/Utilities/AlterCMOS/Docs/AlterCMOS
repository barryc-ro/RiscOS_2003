*AlterCMOS -offset <int> -value <int> [-reset]

Check the value in the specified CMOS location. Exit if it matches
the new value supplied. Otherwise, store the new value in CMOS. If
the '-reset' switch is present, cause the machine to reboot after
the new value is stored.
