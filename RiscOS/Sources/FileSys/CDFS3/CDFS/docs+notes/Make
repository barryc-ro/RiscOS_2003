Makedebug - makes a version with dbmalloc and debugging output enabled
Makefile - makes a non-debugging versions (both the above disabled)
MakeClean should be used in between different kinds of compilation
to clear the object files from the 'o' directory.

Makedebug makes a relocatable module image called CDFSDebug.
Makefile makes a relocatable module image called CDFS.
