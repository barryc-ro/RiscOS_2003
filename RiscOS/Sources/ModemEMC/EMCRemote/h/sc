
#ifndef __sc_h
#define __sc_h

#include "kernel.h"

extern _kernel_oserror *sc_readfile (const char *name, char **buf, size_t *nbytes);
extern _kernel_oserror *sc_readmulti (char **buf, size_t *nbytes);

extern _kernel_oserror *sc_writefile (const char *name, char *buf, size_t nbytes);
extern _kernel_oserror *sc_writemulti (char *buf, size_t nbytes);


#endif
