; > ToDo3

  + Make EPROM image for podule

 Macrovision:
  + Turn off RGB DACs when anti-taping
  + Issue MPEGVideo_CardControl to select composite video on SCART when in
    Macrovision video path

 !MPEGPlay3:
  + Sort out -macrovision mode
  + Sort out icon plotting
  
 MPEG2Video:
  + Implement CardControl SWI

 PAL:
  + Reprogram with version 3(!)
  -> Macrovision: + Podule: Always select Macrovision video path in TDA8540
                          Set SCART to use RGB in non-Macro, CVBS in Macro
                  STB2/2: Select normal or Macrovision in TDA8540
                          Set SCART to use RGB in non-Macro, CVBS in Macro

 ModeFiles:
  + Do bodged version that registers PALTV and NTSCTV files and does LoadModeFile
    of one reading PAL/NTSC bit in podule status register somehow?

 SoakTest:
  * Suss why the video doesn't work (proving obstinate!)

 General:
  * Sort out horizontal positioning/sync fiasco
    + For normal mode
    * For macrovision mode

