#include "Desk.Wimp.h"
#include "^.Desk.Window2.h"

Desk_window_info*	GetInfoAlloc( Desk_window_handle window);
/*
As Desk_Window_GetInfoAlloc, except alters workarea to exclude parts not
scrollable to.
 */

#define	GetInfoFree( info)	Desk_Window_GetInfoFree( info)
