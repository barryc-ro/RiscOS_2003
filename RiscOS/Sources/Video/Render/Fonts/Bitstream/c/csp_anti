/* 16-bit external font anti-aliasing.
	·	Redirect VDU output to Sprite
	·	Create character from Bitstream outline strokes
	·	Anti-alias sprite (16 greyscales)
	·	Store sprite to private, reallocatable area, pointed to
		by structure during creation of font. Realloc area grows
		with every additional sprite to cache limit. If cache limit
		reached, oldest sprite removed and new one added.
*/

void VDU2Sprite( void )
{
}

void VDU2VDU( void )
{
}

void DrawCharacter( void )
{
}

void AACharacter( void )
{
}

int RemoveOldest( void )
{
}

int InsertSprite( void )
{
}
