;
; The table of required settings for Omega machines:
; Entries for mask and value default to &ff and 0 respectively
;
SettingsTable

        ^       0

	Set	FileLangCMOS,		,	46	; ResourceFS
	Set	FrugalCMOS,
	Set	FrugalCMOS+1,
	Set	Unplug12CMOS,
	Set	MODETVCMOS,		,       &10	; TV 0,1
	Set	StartCMOS,		&f8,	&50	; No caps, No dir, power down standby
	Set	KeyDelCMOS,		,	32
	Set	KeyRepCMOS,		,	8
	Set	PSITCMOS,		&02,	2	; Ignore the ignore char
	Set	DBTBCMOS,		&10,	&10	; Boot
	Set	Unplug10CMOS,
	Set	Unplug11CMOS,
	Set	ExtnUnplug1CMOS,
	Set	ExtnUnplug2CMOS,
;       Set	TerritoryCMOS,				; Allowed to be UK or USA
	Set	PrinterBufferCMOS,			; default = 1K in usual area
	Set	FileSwitchCMOS,		&09,	&01	; truncate names
	Set	MouseCMOS,		,	3	; PS2 mouse
;	Set	SparrowMarker,		,	&0f	; we are not RISCiX
;	Set	SparrowMarker+1,
;	Set	SparrowMarker+2,	,	9	; flags
; [ {TRUE}
;	Set	SparrowMarker+3,	,	21	; CtrlServer
;	Set	SparrowMarker+4,	,	&c0
; ]
;	Set	SparrowMarker+5,	,	&aa
;	Set	SparrowMarker+6,	,	&88
; [ {TRUE}
;	Set	SparrowMarker+7,	,	22	; IPAddress
;	Set	SparrowMarker+8,	,	&c0
; ]
;	Set	SparrowMarker+9,	,	&aa
;	Set	SparrowMarker+&a,	,	&88
;	Set	SparrowMarker+&b,
;	Set	SparrowMarker+&c,
;	Set	SparrowMarker+&d,
;	Set	SparrowMarker+&e,
 	Set	Unplug13CMOS,
	Set	Unplug14CMOS,
	Set	PrintSoundCMOS,		,	&A4	; print in GSTrans, tabs valid, 16bit VIDC/44.1KHz interp. sound
	Set	VduCMOS,		,       &FD	; Auto syncs, monitor type auto
	Set	FontCMOS,		,	32	; 128k font cache
;       Set	CDROMFSCMOS,		,	&a1	; 1 CD drive, 128K buffer
;       Set     TimeZoneCMOS,				; Allowed to be 0 or USA zone (EC, E8, E4, E0)
	Set	DesktopFeaturesCMOS,	,	&01	; 3D, tiled, use Wimp${Font,Size}
	Set	Unplug15CMOS,
	Set	Unplug16CMOS,
	Set	ScreenSizeCMOS,
	Set	RAMDiscCMOS,
	Set	SysHeapCMOS,
	Set	RMASizeCMOS,
	Set	SpriteSizeCMOS,
	Set	SoundCMOS,		,	&f0	; voice 1, loudness 7
	Set	MosROMFrugalCMOS,			; system ROM
	Set	MosROMFrugalCMOS+1,
	Set	MosROMFrugalCMOS+2,
	Set	MosROMFrugalCMOS+3,
	Set	PoduleFrugalCMOS,
	Set	PoduleFrugalCMOS+1,
	Set	PoduleFrugalCMOS+2,
	Set	PoduleFrugalCMOS+3,
	Set	PoduleFrugalCMOS+4,
	Set	PoduleFrugalCMOS+5,
	Set	PoduleFrugalCMOS+6,
	Set	PoduleFrugalCMOS+7,
;	Set	LanguageCMOS,				; set by Diagnostic module
;       Set	CountryCMOS,				; Allowed to be UK or USA
	Set	Misc1CMOS,		,	&80	; *run boot file, no cache icon, no screen blank, short POST
	Set	MouseStepCMOS,		,	2
	Set	SystemSpeedCMOS,	&38,	&18	; Power-saving, WimpMode=Auto, cache on
	Set     WimpFlagsCMOS,		,       &EF     ; normal defaults
	Set	FontMaxCMOS,				; No max font cache size
	Set	FontMax1CMOS,				; Rescale bitmaps up to 16pt
	Set	FontMax2CMOS,		,	&3c	; Anti-alias up to 48pt
	Set	FontMax3CMOS,		,	&28	; Cache bitmaps up to 48pt
	Set	FontMax4CMOS,				; Hrz sub-pixel a-a
	Set	FontMax5CMOS,				; Vrt sub-pixel a-a
	Set	Unplug7CMOS,
	Set	Unplug8CMOS,
	Set	Unplug9CMOS,
	Set	Unplug17CMOS,
	Set	&FF					; End of table marker

	END
