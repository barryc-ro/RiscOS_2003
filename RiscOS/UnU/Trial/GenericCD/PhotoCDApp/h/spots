/* Mappings from names to numbers */

typedef struct
{
    char *name;
    int code;
} WordToNumRec, *WordToNumPtr;

extern WordToNumRec keynames [];

extern void spots_convert (char *, int, int *);

extern char *num_to_string (int, WordToNumPtr);
