#include <stdio.h>

#include "TestLib.TestLib.h"


int	TestLib_Factorial( int n)
{
#ifdef DEBUG
	fprintf( stderr, "TestLib_Factorial called for n=%i\n", n);
#endif
if ( n<=2)	return n;
else		return n * TestLib_Factorial( n-1);
}
