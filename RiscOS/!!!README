This is a SrcFiler source tree, and should *ONLY* be accessed using !SrcFiler
(version 0.26 is recommended).

The SrcFiler project entry for this tree is called STBSrc and can be found in
	NFS::spinner.$.Sources.STB.SrcFiler.!Projects

Now go away.

Jon.
